name: Tests 
run-name: ${{ github.actor }} is testing out GitHub Actions
on: [push]
jobs:
	Explore-Github-Actions: 
		runs-on: ubuntu-latest
		steps:
			- python3 -m unittest tests_package/unit/mock_tests.py
			- if {{ success() }}
			- terraform apply
